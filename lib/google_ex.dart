import 'package:flutter/material.dart';

class GoogleEx extends StatelessWidget {
  const GoogleEx({super.key});

  @override
  Widget build(BuildContext context) {
    return const GoogleMap(
      
    );
  }
}